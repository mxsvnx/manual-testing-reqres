# Чек-лист — форма авторизации Reqres.in

**Объект:** POST /api/login
**Версия:** 1.0
**Дата:** 2024-06
**Результаты:** реальные, получены через Postman v10

Обозначения: ✅ прошёл | ❌ не прошёл | ⚠️ поведение отличается от ожидаемого

---

## 1. Позитивные проверки

| # | Проверка | Результат |
|---|----------|-----------|
| 1 | Успешный вход с валидным email и паролем | ✅ |
| 2 | Ответ содержит `token` при успешном входе | ✅ |
| 3 | Статус-код 200 при корректном запросе | ✅ |
| 4 | Время ответа не превышает 2 секунды | ✅ |
| 5 | Email принимается в нижнем регистре | ✅ |
| 6 | Email принимается в верхнем регистре | ⚠️ возвращает 400 — API чувствителен к регистру |

---

## 2. Негативные проверки — поле Email

| # | Проверка | Результат |
|---|----------|-----------|
| 7 | Вход с незарегистрированным email → ошибка | ✅ 400 `"user not found"` |
| 8 | Пустое поле email → ошибка | ✅ 400 `"Missing email or username"` |
| 9 | Email без символа `@` → ошибка | ⚠️ 400, но сообщение `"user not found"` вместо `"Invalid email"` (см. BUG-001, BUG-002) |
| 10 | Email без домена (`user@`) → ошибка | ✅ 400 `"user not found"` |
| 11 | Email без имени пользователя (`@example.com`) → ошибка | ✅ 400 |
| 12 | Email с пробелом внутри → ошибка | ✅ 400 |
| 13 | Email с двумя символами `@` → ошибка | ✅ 400 |
| 14 | Только пробелы в поле email → ошибка | ✅ 400 |

---

## 3. Негативные проверки — поле Password

| # | Проверка | Результат |
|---|----------|-----------|
| 15 | Пустое поле password → ошибка | ✅ 400 `"Missing password"` |
| 16 | Неверный пароль при верном email → ошибка | ✅ 400 `"user not found"` |
| 17 | Только пробелы в поле password → ошибка | ⚠️ 400, но без явного указания на пробелы (см. BUG-003) |

---

## 4. Граничные значения

| # | Проверка | Результат |
|---|----------|-----------|
| 18 | Email длиной 1 символ + `@d.co` | ✅ 400 `"user not found"` (формат принят) |
| 19 | Email длиной 254 символа | ✅ обрабатывается корректно |
| 20 | Email длиной 255 символов | ✅ 400 |
| 21 | Пароль из 1 символа | ✅ обрабатывается, ошибка если пользователь не найден |
| 22 | Пароль из 100 символов | ✅ обрабатывается корректно |

---

## 5. Специальные символы и безопасность

| # | Проверка | Результат |
|---|----------|-----------|
| 23 | Email со спецсимволами (`user+tag@example.com`) | ✅ принимается, 400 если не найден |
| 24 | SQL-инъекция в поле email | ✅ не исполняется, 400 |
| 25 | XSS в поле password | ✅ не исполняется, 400 |
| 26 | Кириллица в поле email | ✅ 400 |
| 27 | Числа в поле email | ✅ корректная обработка |

---

## 6. Формат запроса

| # | Проверка | Результат |
|---|----------|-----------|
| 28 | Запрос без заголовка `Content-Type` | ✅ 400 с ошибкой |
| 29 | Пустое тело запроса `{}` | ✅ 400 `"Missing email or username"` |
| 30 | Поле `email` отсутствует | ✅ 400 `"Missing email or username"` |
| 31 | Поле `password` отсутствует | ✅ 400 `"Missing password"` |
| 32 | Лишние поля в теле (`"role": "admin"`) | ✅ игнорируются, токен выдаётся |

---

## Итог

| Статус | Количество |
|--------|-----------|
| ✅ Прошёл | 27 |
| ⚠️ Отклонение | 3 |
| ❌ Упал | 0 |

Найденные отклонения описаны в `bug-reports.md`.
